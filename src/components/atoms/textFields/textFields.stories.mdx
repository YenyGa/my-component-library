import {Meta, Preview, Story} from "@storybook/addon-docs/blocks";
import {EmailInput, PasswordInput} from "./textFields";

<Meta title="Atoms/TextFields" component={EmailInput} />

# TextFields

TextFields is an **Atom** that allows generating a text field input

## Usage

TextFields are used to allow the user to enter information

## Email Inputs

Email Inputs are used to allow the user to enter emails.

<Preview withToolbar>
  <Story name="email">
    <EmailInput label="Email" placeholder="yeny.garcia@gmail.com" />
  </Story>
</Preview>

## Password Inputs

EmailInputs are used to allow the user to enter passwords.

<Story name="password">
  <PasswordInput label="Password" />
</Story>
