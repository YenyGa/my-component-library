import {Meta, Preview, Story} from "@storybook/addon-docs/blocks";
import {Input} from "./inputs";

<Meta title="Atoms/Inputs" component={Input} />

# Inputs

Input is an **Atom** that allows generating an input

## Usage

Inputs are used to allow the user to enter information

## Inputs

Inputs are used to allow the user to enter information.

<Preview withToolbar>
    <Story name="input">
        <Input type="email" placeholder="yeny.garcia@gmail.com" />
    </Story>
</Preview>
