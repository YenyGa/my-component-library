import {Meta, Preview, Story} from "@storybook/addon-docs/blocks";
import {boolean} from "@storybook/addon-knobs";
import {action} from "@storybook/addon-actions";
import {Modal} from "./modals";
import {H2} from "../labels/labels";

<Meta title="Atoms/Modals" component={Modal} />

# Modals

Modals are dialogs which overlay content to convey.

## Usage

Use modals to call the user to action or complete a task.

<Preview withToolbar>
    <Story name="modal">
        <Modal showModal={boolean("Show Modal", true)} setShowModal={action('modal-closed')}>
            <H2>This is an example of a modal</H2>
        </Modal>
    </Story>
</Preview>
